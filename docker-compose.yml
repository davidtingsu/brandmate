services:
  redis:
    image: redis/redis-stack:latest
    ports:
      # Use 6380 to avoid conflict with a local brew/homebrew redis on 6379
      - "6380:6379"
      - "8001:8001"
    volumes:
      - redis_data:/data
    environment:
      - REDIS_ARGS=--save 60 1

volumes:
  redis_data:
